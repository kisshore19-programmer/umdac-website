import { ApplicationForm } from '@/components/ApplicationForm'

export default function TestApplicationPage() {
  // Replace 1 with a real event_id — see note below
  return <ApplicationForm eventId={1} />
}