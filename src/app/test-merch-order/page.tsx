import { MerchOrderForm } from '@/components/MerchOrderForm'

export default function TestMerchOrderPage() {
  // Replace 1 with a real merch_id — check Table Editor → merch
  return <MerchOrderForm merchId={1} />
}